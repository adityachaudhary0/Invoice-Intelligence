# Freight Cost Model

The trained regression model is **not committed** to this repository.

After cloning the project, place the trained model file here:

```
predict freight_model.pkl
```

Train it locally:

```bash
cd freight_cost_prediction
python train.py
```

Or copy the artifact from your secure model storage.
