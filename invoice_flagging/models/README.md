# Invoice Flagging Models

Trained model artifacts are **not committed** to this repository.

After cloning the project, place these files here:

```
predict_flag_invoice.pkl
scaler.pkl
```

Train them locally:

```bash
cd invoice_flagging
python train.py
```

Or copy the artifacts from your secure model storage.
