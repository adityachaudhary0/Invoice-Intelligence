# Screenshots

Add application screenshots here for the README.

Suggested captures:

1. **home.png** — Streamlit landing page
2. **freight.png** — Freight cost prediction result
3. **invoice.png** — Invoice risk prediction (Normal / Flagged)
4. **api-docs.png** — FastAPI Swagger UI at `/docs`

Capture while both services are running:

```bash
uvicorn main:app --reload
streamlit run frontend/app.py
```
